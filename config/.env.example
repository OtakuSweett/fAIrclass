# Environment variables for fAIrclass
# Copy this file to customize local credentials.

DB_HOST=localhost
DB_USER=fairclass_user
DB_PASS=strong_password_here
DB_NAME=fairclass_db
DB_CHARSET=utf8mb4



# Optional: run environment (development or production)
ENVIRONMENT=production

# Base URL (used as fallback in some environments)
# Example: http://localhost or https://mydomain.com
BASE_URL=http://localhost

# Cloudflare Turnstile (optional - añade tus claves para mostrar el captcha)
# CF_TURNSTILE_SITEKEY: clave pública usada en los formularios (widget)
# CF_TURNSTILE_SECRET: clave privada usada en la verificación server-side
CF_TURNSTILE_SITEKEY=
CF_TURNSTILE_SECRET=

# Notes:
# - Prefer colocar este archivo en config/.env (ya se busca ahí) y no subirlo al repo.
# - Para producción, establece ENVIRONMENT=production y configura HTTPS/BASE_URL apropiado.
